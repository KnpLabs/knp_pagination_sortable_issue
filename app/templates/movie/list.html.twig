{% extends "base.html.twig" %}

{% block body %}
	<main class="container p-4 d-flex flex-wrap">
		<table class="table">
			<thead>
				<tr>
					<th>#</th>
					<th>Title</th>
					<th>Price</th>
				</tr>
			</thead>
			<tbody>
				{% for movie in movies %}
					<tr>
						<th scope="row">{{ movie.id }}</th>
						<td>{{ movie.title }}</td>
						<td>{{ (movie.price/100)|number_format(2, ',', ' ') }}€</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</main>
{% endblock %}
